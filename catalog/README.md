# Legacy intake catalog

Generated by `tools/intake/legacy_catalog.py` (legacy-catalog-v1) at 2026-09-14T14:55:35. Do not edit by hand; regenerate.

Everything the engine and app repos produced before the lab existed, inventoried by content hash so the Phase 0 registry (issues #1, #2) can import it. **No lab identities are assigned here.** Each entry's `proposedKind` says what it would become.

| root | pinned at |
|---|---|
| engine | `91df20b888` |
| app | `83bffd6800` |
| tools | not a git repo |

| file | proposed kind | entries | what |
|---|---|---:|---|
| `models.json` | M | 23 | nets and calibrations: sha256, locations, metadata, exact serialized parameter count, role, provenance completeness |
| `sources.json` | S | 80 | corpora: per-file sha256 and rows (105,865,703 rows, 10.2 GB), sampled fields mapped to provenance classes |
| `evaluations.json` | E | 23 | position suites, opening books, suite sidecars, SPRT gate protocols |
| `evidence.json` | R | 55 | gate records mapped to lab states, plus anchor/efficiency measurements |
| `engines.json` | M + search profile | 1 | engine identity registry (engine `benchmarks/engines.json`) |
| `switches.json` | ablation columns | 1 | generated search-switch registry (engine `benchmarks/search-switches.json`) |
| `manifest.json` | — | 1 | roots, repo commits, counts |

## Findings the lab must resolve at import

- **Provenance:** 22 of 23 models lack `trainingCommit` + `datasetManifestHash`, so their training data cannot be reconstructed from metadata alone. Import them as development artifacts, not promotable models.
- **Roles:** flagship-eval-calibration 1, flagship-helper-net 1, flagship-main-net 1, gen10-experimental 13, gen11-candidate 1, gen7-frozen-baseline 1, gen9-candidate 1, gen9-experimental-ranker 1, unregistered 3.
- **Evidence states:** INCONCLUSIVE 30, REJECTED 12, SUPPORTED 11, SUPPORTED-AS-MEASUREMENT 2. Superseded 2026-09-09 records are listed as recorded; engine `benchmarks/INV1_GATE_INTEGRITY_2026-09-09.md` says which were superseded.
- **Ambiguous labels:** 66 sources have a `cp` or other field whose authority depends on the producing pipeline. The first N#### normalization must assign each an explicit provenance class.
- **Duplicates:** identical bytes appear once, with every location listed. Row-level duplicates across corpora (for example gen9-train vs gen9-cvs) are not detected here; that belongs to canonical record identity (#2).
